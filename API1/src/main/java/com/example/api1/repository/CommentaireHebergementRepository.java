package com.example.api1.repository;

import com.example.api1.api.model.CommentaireHebergement;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommentaireHebergementRepository extends JpaRepository<CommentaireHebergement, Integer> {
}
